export function HomeLeadIdea() {
  return (
    <section className="shell py-24 sm:py-32">
      <h2 className="display max-w-[10ch] text-[48px] sm:text-[72px] lg:text-[84px]">
        Un lead no es un cliente.
      </h2>
      <p className="mt-6 max-w-[34ch] text-[18px] text-muted">
        Es el comienzo de una operación que DuMo hace visible.
      </p>
    </section>
  );
}
